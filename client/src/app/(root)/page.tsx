import HomePage from "@/components/HomePage";

const Home = async () => {
  return (
    <section className="p-2 h-screen w-screen flex flex-col justify-center items-center gap-1">
      <HomePage />
    </section>
  );
};

export default Home;
